/**
 * Historical extractor from the original HTML tracker.
 * `src/tracker/tracker-app.ts` is now the source of truth (auth, invites, DB sync).
 * Do not regenerate it from tracker-app.patched.js.
 */
console.error(
  "tracker-app.ts is maintained directly. Refusing to overwrite it from tracker-app.patched.js.",
);
process.exit(1);
