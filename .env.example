# Tracker UI is a BFF — APIs live on workforce-backend.
BACKEND_API_BASE_URL=http://localhost:4000/api/v1

# Optional public base if absolute URLs are needed in the browser.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1

# Edge cookie gate still checks tracker_access / tracker_refresh cookie presence.
# JWT_SECRET is no longer required for Next runtime (tokens are workforce JWTs).
