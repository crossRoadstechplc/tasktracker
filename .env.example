# Local Postgres: npm run db:up  (docker compose) then npm run db:migrate && npm run db:seed
DATABASE_URL="postgresql://postgres:password@localhost:5432/spx_task_tracker"

# Neon LISTEN/NOTIFY for SSE requires a direct (non-pooler) connection.
# Optional: set explicitly if auto-deriving from DATABASE_URL fails.
# DIRECT_URL="postgresql://user:pass@ep-xxx.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require"

JWT_SECRET="replace-with-a-long-random-secret-at-least-32-characters"

SMTP_HOST=gvam1219.siteground.biz
SMTP_PORT=465
SMTP_USER=noreply@spxafrica.com
SMTP_PASS=
SMTP_FROM="Workforce <noreply@spxafrica.com>"
ADMIN_PORTAL_URL=http://localhost:3000
INVITE_TTL_HOURS=72
